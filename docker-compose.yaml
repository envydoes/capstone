services:
  server:
    build:
      context: .
    environment:
      - DB_HOST=${DB_HOST}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${DB_NAME}
    volumes:
      - uploads:/var/www/html/uploads
    ports:
      - 80
volumes:
  uploads: